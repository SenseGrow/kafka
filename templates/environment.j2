NAME=kafka

USER={{ sansible_kafka_user }}
GROUP={{ sansible_kafka_group }}

KAFKA_HEAP_OPTS="{{ sansible_kafka_heap_opts }}"
JMX_PORT="{{ sansible_kafka_jmx_port }}"
MAX_OPEN_FILES="{{ sansible_kafka_max_open_files }}"

{% for key, value in sansible_kafka_environment_vars.iteritems() %}
{{key}}={{value}}
{% endfor %}
